#include "main.h"
/**
 * _islower - check character is lowercase
 * @c: parametre character
 * Return: 1 if lowercase or 0 is not
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
		return (0);

}
