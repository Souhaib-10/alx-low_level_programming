#include "main.h";
/**
 * _abs- Entry point
 * Description:computes the absolute value of an integer
 * @n: parameter
 * Return: absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
