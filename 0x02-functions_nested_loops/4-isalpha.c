#include "main.h"
/**
 * _isalpha - for alphabetic character.
 * @c: parametre character
 * Return: 1 if alphabitic or 0 is not
 */
int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z' (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
		return (0);

}
